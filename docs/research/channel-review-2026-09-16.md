# 频道推送修正与历史验证（2026-09-16）

## 执行边界

- 按最新要求停止补推；历史修正只调用 `editMessageText` / Telegraph `editPage`。
- Telegram 不显示独立的 English / 中文标题，仍保留双语内容。
- 不以重新发布替代原地编辑；保留原消息 ID、顺序与文章链接。
- 审核完成后按用户要求提交、推送并部署；保留用户原有 CLAUDE.md、AGENTS.md 变更，不纳入本次提交。

## 修正

- Codex：Atom 无稳定 CLI 版本时回退 REST；区分 CLI / SDK tag；不降级；遇到旧的非 CLI 状态停止发送并要求核对。
- OpenClaw：以正式 GitHub Release 为发布门槛，支持拆分后的 CHANGELOG，避免推送未发布内容及空补丁日志。
- Hermes：保留升级、迁移及数据库恢复操作说明。
- Telegram：补全官方来源链接，移除重复版本标题和语言标题，修正文件名自动链接、代码占位符、CRLF 围栏、标题内粗体。
- Telegraph：修复引用、嵌套列表、代码标识符、尖括号参数及单词间空白；使用原地编辑并回读核验。
- 翻译：约束权限语义、图片与容器镜像、模型名称解析、提供商标识；防止摘要凭空添加 Breaking / 安全标签。

## 重复消息事故

生产旧状态为 `python-v0.154.0`，REST 回退发现 `rust-v0.154.0` 后，定时任务错误地发送了同版本 CLI 消息 261。
已备份并删除该重复消息，保留原消息 259，修复生产消息状态及非 CLI 旧状态保护。
删除的是公开重复消息；备份保存其内容，无法恢复原 Telegram 消息 ID。

## 验证范围

| 项目 | 结果 |
| --- | --- |
| 公开历史快照 | 709 条：Claude 396、Codex 163、OpenClaw 115、Hermes 35（含服务消息） |
| 单元测试 | 171 项通过 |
| Codex 官方历史回放 | 155 版：154 通过，1 版官方原文为空；670 个代码字面量检查通过 |
| OpenClaw 官方发布历史 | 获取 243 条 release，105 个稳定版本通过来源、版本、非空及长度检查 |
| Claude / Hermes 文章渲染回放 | 396 / 34 版，检查 4,409 / 1,852 个行内代码字面量 |
| 原始消息本地渲染 | 709 条，无 TG 占位符泄漏 |
| Telegram 原地修正 | 562 / 562 成功；两条网络失败记录单独重试通过；全部结果对应当前修改计划 |
| 来源链接 | 核验 551 个唯一链接，对失效目标使用已验证替代来源 |
| 翻译语义 | 8 个重点样本；不代表逐句验证了所有历史译文 |
| Telegraph 原地修正 | 258 篇，回读匹配计划；1,597 处代码及 12,242 处空白修复 |
| 生产只读模拟 | 四个 checker 均退出 0，新消息调用次数均为 0；Codex 首次网络错误后重试通过 |
| 最新公开消息 | Claude 601、Codex 259、OpenClaw 124、Hermes 38 均无独立语言标题；重复消息 261 已不存在 |
| 最终全量公开回读 | 709 条消息 ID 集合与原快照相同，无新增或缺失；无独立 English / 中文标题；OpenClaw 124 保留新文章链接 |

详尽快照、修改前备份、逐条 API 结果及验证数据位于本地 `output/channel-review/`。
历史回放只执行读取和本地处理，不向频道发送验证消息。

## 限制

旧文章 https://telegra.ph/Claude-Code-210-Released-01-08 不属于当前 Telegraph 账号，无法原地编辑。
原文保持不变；关联 Telegram 消息 379 已补官方来源链接。未创建替代文章或通知。

修复已同步至 `rn-vps:/opt/ai-release-tracker` 并重建镜像，保留 `.env`、`output/` 及运行状态。
本轮未向 WSL 同步源码。
